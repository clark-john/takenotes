# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type User {
  id: ID!
  firstName: String!
  lastName: String
  username: String!
  createdAt: String
}

type AccessToken {
  accessToken: String!
  user: User!
}

type Note {
  id: ID!
  content: String!
  backgroundColor: String!
  notebookId: ID!
  userId: ID!
  saved: Boolean
  createdAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Notebook {
  id: ID!
  name: String!
  description: String!
  backgroundColor: ID
  userId: ID!
  saved: Boolean!
  createdAt: DateTime!
}

type Query {
  getSavedNotes: [Note!]!
  getSavedNotebooks: [Notebook!]!
  me: User!
  hello: String!
  getNotes(notebookId: String!): [Note!]!
  getNotebooks: [Notebook!]!
  getNotebookInfo(id: String!): Notebook
  getNote(id: String!): Note
}

type Mutation {
  saveNotebook(id: String!): Notebook!
  unsaveNotebook(id: String!): Notebook!
  saveNote(id: String!): Note!
  unsaveNote(id: String!): Note!
  login(login: Login!): AccessToken!
  register(register: Register!): AccessToken!
  logout: String!
  createNotebook(notebook: AddNotebook!): Notebook!
  deleteNotebook(id: String!): Notebook!
  updateNotebook(notebook: UpdateNotebook!): Notebook!
  createNote(note: AddNote!): Note!
  createBlankNote(notebookId: String!): Note!
  updateNote(note: UpdateNote!): String!
  deleteNote(id: String!): Note!
}

input Login {
  username: String!
  password: String!
}

input Register {
  firstName: String!
  lastName: String
  username: String!
  password: String!
  actualPassword: String
  createdAt: DateTime
}

input AddNotebook {
  name: String!
  description: String!
}

input UpdateNotebook {
  name: String
  description: String
  id: ID!
}

input AddNote {
  content: String!
  notebookId: ID!
  backgroundColor: String
}

input UpdateNote {
  content: String
  backgroundColor: String
  id: ID!
}