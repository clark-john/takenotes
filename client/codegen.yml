schema: http://localhost:8000/graphql
documents: [
  src/graphql/*.graphql,
  src/graphql/*.gql
]
generates:
  src/generated.ts:
    plugins: 
      - typescript
      - typescript-operations
      - typescript-document-nodes
    config:
      nameSuffix: document
