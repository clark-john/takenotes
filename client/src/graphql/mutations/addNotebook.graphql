mutation AddNotebook($name: String!, $description: String!) {
	createNotebook(notebook: {
		name: $name
		description: $description	
	}) {
		id
		name
		description
		createdAt
		# photoId
		backgroundColor
		userId
	}
}
